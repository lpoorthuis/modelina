{
  "$id": "JacksonOrder",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "order_id": {
      "type": "string"
    },
    "status": {
      "$id": "OrderStatus",
      "type": "string",
      "enum": ["in-progress", "done"]
    }
  },
  "required": ["order_id", "status"]
}
